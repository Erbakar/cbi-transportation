import {sqliteTable,text,integer,uniqueIndex,index} from 'drizzle-orm/sqlite-core';
export const records=sqliteTable('records',{id:text('id').primaryKey(),owner:text('owner').notNull(),filename:text('filename').notNull(),hash:text('hash').notNull(),objectKey:text('object_key').notNull(),mime:text('mime').notNull(),createdAt:text('created_at').notNull(),updatedAt:text('updated_at').notNull(),revision:integer('revision').notNull().default(1),status:text('status').notNull(),fields:text('fields'),issues:text('issues'),extraction:text('extraction'),lockUntil:integer('lock_until').notNull().default(0)},t=>[uniqueIndex('records_owner_hash').on(t.owner,t.hash),index('records_owner_created').on(t.owner,t.createdAt)]);
export const revisions=sqliteTable('revisions',{id:text('id').primaryKey(),recordId:text('record_id').notNull(),revision:integer('revision').notNull(),filename:text('filename').notNull(),hash:text('hash').notNull(),objectKey:text('object_key').notNull(),createdAt:text('created_at').notNull()},t=>[index('revisions_record').on(t.recordId)]);
export const deliveries=sqliteTable('deliveries',{id:text('id').primaryKey(),recordId:text('record_id').notNull(),owner:text('owner').notNull().default(''),businessKey:text('business_key'),platform:text('platform').notNull(),kind:text('kind').notNull(),status:text('status').notNull(),reference:text('reference'),message:text('message'),idempotencyKey:text('idempotency_key').notNull(),updatedAt:text('updated_at').notNull(),payload:text('payload')},t=>[uniqueIndex('delivery_record_platform').on(t.recordId,t.platform),uniqueIndex('delivery_business').on(t.owner,t.platform,t.businessKey)]);
export const sessions=sqliteTable('sessions',{hash:text('hash').primaryKey(),owner:text('owner').notNull(),expires:integer('expires').notNull()});
export const loginAttempts=sqliteTable('login_attempts',{key:text('key').primaryKey(),count:integer('count').notNull(),expires:integer('expires').notNull()});
export const platformSessions=sqliteTable('platform_sessions',{key:text('key').primaryKey(),encrypted:text('encrypted').notNull(),expires:integer('expires').notNull()});
