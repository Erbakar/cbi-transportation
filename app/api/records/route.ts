import {requireUser} from '@/lib/server/auth';
import {AppError,db,digest,errorResponse,json,runtime,sameOrigin} from '@/lib/server/runtime';
import {detectFile,owned,Row,view,limitedForm} from '@/lib/server/records';
import {allTargets} from '@/lib/server/platforms';
export async function GET(req:Request){try{const owner=await requireUser(req);const {results}=await db().prepare('SELECT * FROM records WHERE owner=? ORDER BY created_at DESC LIMIT 500').bind(owner).all<Row>();return json({records:await Promise.all(results.map(view))})}catch(e){return errorResponse(e)}}
export async function POST(req:Request){let heldId='',heldOwner='',lease=0;try{sameOrigin(req);const owner=await requireUser(req);if(Number(req.headers.get('content-length')||0)>11*1024*1024)throw new AppError('Dosya en fazla 10 MB olabilir.',413);const form=await limitedForm(req);const file=form.get('file');if(!(file instanceof File)||!file.size)throw new AppError('Bir talimat dosyası seçin.');if(file.size>10*1024*1024)throw new AppError('Dosya en fazla 10 MB olabilir.',413);const bytes=await file.arrayBuffer(),mime=detectFile(new Uint8Array(bytes),file.name),hash=await digest(bytes);const existing=await db().prepare('SELECT * FROM records WHERE owner=? AND hash=?').bind(owner,hash).first<Row>();if(existing)return json({record:await view(existing),duplicate:true});const replacement=form.get('recordId');const id=typeof replacement==='string'&&replacement?replacement:crypto.randomUUID();let previous:Row|null=null;if(replacement){previous=await owned(id,owner);lease=Date.now()+180000;const acquired=await db().prepare('UPDATE records SET lock_until=? WHERE id=? AND owner=? AND lock_until<=?').bind(lease,id,owner,Date.now()).run();if(!acquired.meta.changes)throw new AppError('Talimat halen işleniyor.',409);heldId=id;heldOwner=owner;const check=await db().prepare("SELECT id FROM deliveries WHERE record_id=? AND status IN ('created','sending','unknown')").bind(id).first();if(check)throw new AppError('Aktarılmış veya işlenmekte olan talimat değiştirilemez.',409)}const now=new Date().toISOString(),revision=(previous?.revision||0)+1;const objectKey=`${owner}/${id}/${revision}-${hash}`;if(!runtime().BUCKET)throw new AppError('Dosya deposu henüz yapılandırılmadı.',503);await runtime().BUCKET.put(objectKey,bytes,{httpMetadata:{contentType:mime}});try{const mutations=[];if(previous)mutations.push(db().prepare("UPDATE records SET filename=?,hash=?,object_key=?,mime=?,updated_at=?,revision=?,status='uploaded',fields=NULL,issues=NULL,extraction=NULL WHERE id=? AND owner=? AND lock_until=?").bind(file.name,hash,objectKey,mime,now,revision,id,owner,lease));else mutations.push(db().prepare('INSERT INTO records (id,owner,filename,hash,object_key,mime,created_at,updated_at,revision,status) VALUES (?,?,?,?,?,?,?,?,?,?)').bind(id,owner,file.name,hash,objectKey,mime,now,now,revision,'uploaded'));mutations.push(db().prepare('INSERT INTO revisions (id,record_id,revision,filename,hash,object_key,created_at) VALUES (?,?,?,?,?,?,?)').bind(crypto.randomUUID(),id,revision,file.name,hash,objectKey,now));for(const t of allTargets())mutations.push(db().prepare("INSERT INTO deliveries (id,record_id,platform,kind,status,idempotency_key,updated_at) VALUES (?,?,?,?,?,?,?) ON CONFLICT(record_id,platform) DO UPDATE SET status='waiting',reference=NULL,message=NULL,payload=NULL,business_key=NULL,idempotency_key=excluded.idempotency_key,updated_at=excluded.updated_at").bind(crypto.randomUUID(),id,t.id,t.kind,'waiting',`${id}:${revision}:${t.id}`,now));await db().batch(mutations)}catch(e){await runtime().BUCKET.delete(objectKey);throw e}return json({record:await view(await owned(id,owner)),duplicate:false},201)}catch(e){return errorResponse(e)}finally{if(heldId)await db().prepare('UPDATE records SET lock_until=0 WHERE id=? AND owner=? AND lock_until=?').bind(heldId,heldOwner,lease).run()}}
