import {requireUser} from '@/lib/server/auth';
import {inttraRequest} from '@/lib/server/inttra-workflow';
import {decodeOptions} from '@/lib/inttra-reference';
import {AppError,errorResponse,json} from '@/lib/server/runtime';
export async function GET(req:Request){try{const owner=await requireUser(req);const q=new URL(req.url).searchParams.get('q');if(q!==null){if(q.trim().length<3||q.length>100)throw new AppError('Konum araması 3–100 karakter olmalı.');const r=await inttraRequest(owner,'/siact/geographySi',q) as {cities?:string[][]};if(!Array.isArray(r.cities))throw new AppError('Konum yanıtı okunamadı.',502);return json({locations:r.cities.map(([label,value])=>({label,value}))});}const data=await inttraRequest(owner,'/siact/createPageParams',null) as Record<string,unknown>;return json({carriers:decodeOptions(data.carriersList),containerTypes:decodeOptions(data.containerTypeList),packageTypes:decodeOptions(data.packageTypesList),countries:decodeOptions(data.countriesList).filter(c=>c.value.trim())});}catch(e){return errorResponse(e)}}
