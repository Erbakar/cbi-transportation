import type {InttraSnapshot as Snapshot} from '@/lib/inttra-snapshot';
export function InttraSnapshot({snapshot}:{snapshot:Snapshot}){
 return <section className="snapshot-panel"><h3>INTTRA’ya gönderilen bilgiler</h3><p>SI No: <strong>{snapshot.reference}</strong> · Gönderim anındaki kayıt</p>{snapshot.issues.length>0&&<div className="notice"><strong>Bu gönderimde kontrol edilmesi gereken alanlar var</strong><ul className="quality-list">{snapshot.issues.map(issue=><li key={issue}>{issue}</li>)}</ul><p>Form iyileştirmeleri sonraki gönderimler için geçerlidir. Bu SI değiştirilmedi ve yeniden gönderilmedi.</p></div>}<details><summary>Gönderilen alanları incele</summary><p>Ödeme yöntemi: {snapshot.paymentMethod}</p>{snapshot.parties.map(p=><div className="party-card" key={p.role}><h4>{p.role}</h4><p>{p.name}</p><pre>{[p.street||'Sokak: boş',p.city||'Şehir: boş',p.country||'Ülke: boş',p.postalCode].filter(Boolean).join('\n')}</pre></div>)}{snapshot.cargo.map((c,i)=><div className="cargo-card" key={i}><h4>{c.container}</h4><p>{c.packages} kap · {c.weight} kg · HS: {c.hs}</p><strong>Cargo Description</strong><pre>{c.description}</pre><strong>Marks & Numbers</strong><pre>{c.marks||'Boş'}</pre></div>)}</details></section>;
}
